@if (AlertModel.Visible)
{
    <div class="alert alert-@AlertModel.Type alert-dismissible fade show" role="alert">
        <strong>@AlertModel.Type.ToUpper()!</strong> @((MarkupString)AlertModel.Message)
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close" @onclick="@(() => AlertModel.Visible = false)"></button>
    </div>
    @*<div class="alert alert-@AlertModel.Type alert-dismissible m-3 p-2" role="alert">
        <h4 class="alert-heading">
            @AlertModel.Type.ToUpper()!
            <button type="button" class="btn-close p-2" data-bs-dismiss="alert" aria-label="Close" @onclick="@(() => AlertModel.Visible = false)"></button>
        </h4>
        <hr>
        <p class="mb-0">
            @((MarkupString)AlertModel.Message)
        </p>
    </div>*@
}

@code {
    [Parameter]
    public AlertBO AlertModel { get; set; }
}
